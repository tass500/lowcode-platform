# .NET
**/bin/
**/obj/
**/TestResults/

# Repo-root CI scratch (e.g. local CycloneDX SBOM output under artifacts/sbom)
artifacts/

# Alternate dotnet output roots under backend/ (dotnet build -o …, teszt/CI artefaktok)
# — ezek NEM ugyanaz, mint bin/obj; nélkülük 10k+ „U” fájl jelenhet meg a Source Controlban
backend/ef-*/
backend/build-*/
backend/bin-verify-*/
backend/test-*/
backend/*-tmp/

# SQLite dev db (if created locally)
**/*.db
**/*.db-shm
**/*.db-wal

# Node / Angular
**/node_modules/
frontend/dist/

backend/*.db
backend/*.db-*

# Logs
**/*.log

# OS/editor
.DS_Store
.vscode/
.idea/

# Local PR body draft for `gh pr create --body-file` (see docs/templates/pr-body.example.md)
/pr-body.md

# Local secrets (GitHub token for gh scripts / optional tooling)
.env
.env.local
!.env.example
